meta {
  name: Update inspection step status
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/api/inspection-steps/{{inspectionStepId}}/status
  body: text
  auth: inherit
}

body:text {
  FAILED
}

settings {
  encodeUrl: true
  timeout: 0
}
