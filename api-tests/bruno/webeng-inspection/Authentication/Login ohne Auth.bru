meta {
  name: Login ohne Auth
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "inspector",
    "password": "inspector123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
