meta {
  name: Create inspection
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/inspections
  body: json
  auth: basic
}

headers {
  Content-Type: application/json
}

auth:basic {
  username: admin
  password: admin123
}

body:json {
  {
    "checklistId": 1,
    "title": "Inspektion Turbine A - Januar",
    "plantName": "Werk Mannheim",
    "plannedDate": "2025-01-15T10:00:00",
    "generalComment": "Regelmäßige Monatsinspektion.",
    "assignedInspectorId": 2
  }
}

tests {
  if (res.body && res.body.id) {
    bru.setEnvVar("inspectionId", res.body.id);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
